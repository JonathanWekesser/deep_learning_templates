services:
  jupyter:
    container_name: torch_cpu
    extends:
      service: jupyter
      file: docker-compose.base.yml
    build: 
      context: .
      dockerfile: torch.cpu.dockerfile
    devices: 
      - /dev/kfd
      - /dev/dri
